#include <stdio.h>

int main(void)
{
	int p_numbers = numbers;	
	int x, y; 
	int numbers[10][10];
	for (x = 0; x<=11; x++){
		for (y = 0; y<=11; y++){
			if (x == 0{
				if(y ==0){
					numbers[x][y] = "*";
				}else{
					numbers[x][y] = 1 * y
				}
			}
			else if (x != 0 && y == 0){
				numbers[x][y] = x * 1
			}else{
				numbers[x][y] = x * y;			
			}
			printf("%d", numbers[x][y]);
		
		}
		printf("\n");
	}	
}


